# Contributing

- Code style: ESLint + TypeScript strict mode
- Tests: Vitest (`npm test`, `npm run test:coverage`)
- Coverage target: 80%+
- Branching: feature branches + PRs
- Releases: tag with `vMAJOR.MINOR.PATCH` and generate GitHub release notes

See [Test Coverage](TEST_COVERAGE.md).
